[project]
name = "autumn-job-tracker"
version = "0.2.0"
description = "Sync recruitment emails to Lark Base and create native Coremail deadline todos"
requires-python = ">=3.11"

[project.scripts]
autumn-tracker = "autumn_tracker.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
