[project]
name = "autumn-job-tracker"
version = "0.3.0"
description = "Classify recruitment emails into a Lark Base with native Coremail deadline todos"
requires-python = ">=3.11"

[project.scripts]
autumn-tracker = "autumn_tracker.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
