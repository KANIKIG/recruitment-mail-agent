[project]
name = "autumn-job-tracker"
version = "0.6.0"
description = "Classify recruitment emails into Lark Base, Coremail todos, and Lark Calendar"
requires-python = ">=3.11"

[project.scripts]
autumn-tracker = "autumn_tracker.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
